package com.nimbus.api.ticket;

/** What the ticket is asking for. */
public enum TicketKind {
    /** Something is broken or confusing and the user needs help. */
    HELP,
    /** A change or new capability is being requested. */
    CHANGE
}
